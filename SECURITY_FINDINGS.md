# Security Findings

- Removed committed secret file `backend/.env.example`.
- Removed committed secret file `backend/.env.example`.

Recommendation: rotate any exposed credentials and keep secrets out of the repo.
